[phases.setup]
nixPkgs = ["nodejs_20", "postgresql"]

[phases.install]
cmds = ["cd backend && npm ci --production=false"]

[phases.build]
cmds = ["cd backend && npx prisma generate && npx tsc -p tsconfig.build.json"]

[start]
cmd = "cd backend && npm run prisma:migrate && npm start"
