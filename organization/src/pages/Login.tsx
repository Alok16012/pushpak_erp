import { useState } from "react";
import { Navigate } from "react-router-dom";
import { useAuth } from "@/contexts/AuthContext";
import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { AlertCircle, ArrowRight, Loader2, LockKeyhole, PlayCircle } from "lucide-react";
export default function Login(){const {user,login,loginDemo}=useAuth();const [identifier,setIdentifier]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{await login(identifier,password)}catch(err){setError(err instanceof Error?err.message:"Unable to sign in")}finally{setBusy(false)}};
return <main className="grid min-h-screen place-items-center bg-[#0c0c10] p-5 text-white"><div className="w-full max-w-md"><div className="mb-8 flex items-center gap-3"><img src="/idealdigiskills-logo.png" className="h-14 w-14 rounded-2xl bg-white object-contain"/><div><p className="text-lg font-semibold">Idealdigiskills ERP</p><p className="text-sm text-white/50">Secure institutional workspace</p></div></div><form onSubmit={submit} className="rounded-[1.75rem] border border-white/10 bg-white/[.06] p-6 shadow-2xl backdrop-blur-xl sm:p-8"><div className="mb-7 grid h-12 w-12 place-items-center rounded-2xl bg-[#c7ff2f] text-black"><LockKeyhole/></div><h1 className="text-3xl font-semibold tracking-[-.04em]">Welcome back</h1><p className="mt-2 text-sm text-white/55">Sign in with your authorised organisation account.</p>{error&&<div className="mt-5 flex gap-2 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-300"><AlertCircle className="h-4 w-4 shrink-0"/>{error}</div>}<div className="mt-7 space-y-5"><div className="space-y-2"><Label htmlFor="identifier">Email or username</Label><Input id="identifier" autoComplete="username" value={identifier} onChange={e=>setIdentifier(e.target.value)} required className="border-white/15 bg-white/5 text-white"/></div><div className="space-y-2"><Label htmlFor="password">Password</Label><Input id="password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required className="border-white/15 bg-white/5 text-white"/></div><Button disabled={busy} className="w-full">{busy?<Loader2 className="animate-spin"/>:<>Sign in<ArrowRight/></>}</Button></div><div className="mt-7 border-t border-white/10 pt-6"><Button type="button" variant="outline" onClick={loginDemo} className="w-full border-white/20 bg-transparent text-white hover:bg-white/10 hover:text-white"><PlayCircle/>Explore the demo</Button><p className="mt-3 text-center text-xs text-white/40">Sample data, no account needed. Nothing you change is saved.</p></div></form><p className="mt-5 text-center text-xs text-white/35">Access is logged and monitored for institutional security.</p></div></main>}
