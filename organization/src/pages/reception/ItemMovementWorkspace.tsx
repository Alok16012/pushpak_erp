import { useEffect, useMemo, useState } from "react";
import { AppLayout } from "@/components/layout/AppLayout";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { ArrowDownLeft, ArrowLeft, ArrowRight, ArrowUpRight, Check, Filter, MoreHorizontal, Package, Plus, Save, Search, SlidersHorizontal, X } from "lucide-react";
import { useToast } from "@/hooks/use-toast";

const movements=[
 {id:"MOV-2081",direction:"Received",item:"Office stationery",party:"ABC Suppliers",qty:100,department:"Administration",status:"Completed",date:"Today, 11:30 AM"},
 {id:"MOV-2080",direction:"Dispatched",item:"Student certificates",party:"ABC Institute",qty:25,department:"Examination",status:"In transit",date:"Today, 10:00 AM"},
 {id:"MOV-2079",direction:"Received",item:"Lab chemicals",party:"Scientific Supplies Co.",qty:15,department:"Science",status:"Needs review",date:"Today, 9:45 AM"},
 {id:"MOV-2078",direction:"Dispatched",item:"Reference books",party:"Central Library",qty:50,department:"Library",status:"Pending pickup",date:"Yesterday"},
];
const stages=["Movement","Item","Logistics","Review"];
type Draft={direction:string;item:string;description:string;qty:string;party:string;phone:string;department:string;courier:string;tracking:string;notes:string};
const blank:Draft={direction:"",item:"",description:"",qty:"",party:"",phone:"",department:"",courier:"",tracking:"",notes:""};

export default function ItemMovementWorkspace(){
 const {toast}=useToast(); const [mode,setMode]=useState<"list"|"form">("list"); const [stage,setStage]=useState(0);const [query,setQuery]=useState("");const [direction,setDirection]=useState("all");
 const [draft,setDraft]=useState<Draft>(()=>{try{return {...blank,...JSON.parse(localStorage.getItem("movement-draft")||"{}")}}catch{return blank}});
 const set=(k:keyof Draft,v:string)=>setDraft(p=>({...p,[k]:v})); const filled=Object.values(draft).filter(Boolean).length;const progress=Math.round(filled/Object.keys(draft).length*100);
 useEffect(()=>localStorage.setItem("movement-draft",JSON.stringify(draft)),[draft]);
 const rows=useMemo(()=>movements.filter(r=>(direction==="all"||r.direction===direction)&&Object.values(r).join(" ").toLowerCase().includes(query.toLowerCase())),[query,direction]);
 const save=()=>toast({title:"Movement saved as draft",description:`${progress}% complete · safe to continue later.`});
 const submit=()=>{toast({title:"Movement recorded",description:`${draft.direction||"Item"} entry was added to the shared register.`});setDraft(blank);setMode("list");setStage(0);localStorage.removeItem("movement-draft")};
 return <AppLayout><div className="mb-5 flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-muted-foreground">Reception</p><h1 className="mt-1 text-3xl font-semibold tracking-[-.04em]">Item movement</h1><p className="mt-1 text-sm text-muted-foreground">A single register for everything received and dispatched.</p></div>{mode==="list"?<Button onClick={()=>setMode("form")}><Plus/>Record movement</Button>:<Button variant="outline" onClick={()=>setMode("list")}><X/>Close form</Button>}</div>
 {mode==="list"?<><div className="mb-4 grid gap-3 sm:grid-cols-3">{[{l:"Received today",v:"12",i:ArrowDownLeft},{l:"Dispatched today",v:"8",i:ArrowUpRight},{l:"Need attention",v:"3",i:Package}].map(x=><Card key={x.l}><CardContent className="flex items-center justify-between p-4"><div><p className="text-xs text-muted-foreground">{x.l}</p><p className="mt-1 text-2xl font-semibold">{x.v}</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-muted"><x.i className="h-4 w-4"/></span></CardContent></Card>)}</div><Card><CardContent className="p-0"><div className="flex flex-col gap-3 border-b p-4 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search item, party, tracking or department…" className="pl-9"/></div><Select value={direction} onValueChange={setDirection}><SelectTrigger className="md:w-44"><Filter className="mr-2 h-4 w-4"/><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All movements</SelectItem><SelectItem value="Received">Received</SelectItem><SelectItem value="Dispatched">Dispatched</SelectItem></SelectContent></Select><Button variant="outline"><SlidersHorizontal/>More filters</Button></div><div className="overflow-x-auto"><table className="w-full min-w-[780px] text-sm"><thead><tr className="border-b bg-muted/35 text-left text-xs text-muted-foreground"><th className="px-4 py-3 font-medium">Movement</th><th className="px-4 py-3 font-medium">Item</th><th className="px-4 py-3 font-medium">Party</th><th className="px-4 py-3 font-medium">Department</th><th className="px-4 py-3 font-medium">Status</th><th className="px-4 py-3 font-medium">Time</th><th/></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-b last:border-0 hover:bg-muted/25"><td className="px-4 py-3"><span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-xs font-semibold ${r.direction==="Received"?"bg-emerald-500/12 text-emerald-600":"bg-violet-500/12 text-violet-600"}`}>{r.direction==="Received"?<ArrowDownLeft className="h-3 w-3"/>:<ArrowUpRight className="h-3 w-3"/>}{r.direction}</span></td><td className="px-4 py-3"><p className="font-medium">{r.item}</p><p className="text-xs text-muted-foreground">{r.id} · Qty {r.qty}</p></td><td className="px-4 py-3">{r.party}</td><td className="px-4 py-3 text-muted-foreground">{r.department}</td><td className="px-4 py-3"><span className="rounded-full bg-muted px-2.5 py-1 text-xs">{r.status}</span></td><td className="px-4 py-3 text-muted-foreground">{r.date}</td><td><Button variant="ghost" size="icon"><MoreHorizontal/></Button></td></tr>)}</tbody></table></div></CardContent></Card></>:<Card className="overflow-hidden"><div className="border-b bg-muted/25 px-5 py-4"><div className="mb-3 flex items-center justify-between"><div><p className="text-sm font-semibold">Record item movement</p><p className="text-xs text-muted-foreground">Draft saves automatically</p></div><strong className="text-sm">{progress}% complete</strong></div><div className="h-2 overflow-hidden rounded-full bg-muted"><div className="h-full bg-brand transition-all duration-500" style={{width:`${progress}%`}}/></div><div className="mt-4 flex gap-2 overflow-x-auto">{stages.map((s,i)=><button key={s} onClick={()=>setStage(i)} className={`flex min-w-[140px] items-center gap-2 rounded-xl border px-3 py-2 text-xs transition ${stage===i?"border-foreground bg-foreground text-background":i<stage?"bg-brand/12":"bg-card text-muted-foreground"}`}><span className="grid h-6 w-6 place-items-center rounded-full bg-muted/60">{i<stage?<Check className="h-3 w-3"/>:i+1}</span>{s}</button>)}</div></div><CardContent className="p-5 sm:p-7"><div className="mx-auto max-w-3xl animate-slide-up">{stage===0&&<Step title="Which way is it moving?" sub="One register, with direction captured at the start."><button onClick={()=>set("direction","Received")} className={`group rounded-2xl border p-5 text-left transition hover:-translate-y-0.5 hover:shadow-lg ${draft.direction==="Received"?"border-emerald-500 bg-emerald-500/10":"bg-card"}`}><ArrowDownLeft className="mb-5 h-6 w-6 text-emerald-500"/><p className="font-semibold">Received</p><p className="mt-1 text-sm text-muted-foreground">An item has arrived at the institution</p></button><button onClick={()=>set("direction","Dispatched")} className={`group rounded-2xl border p-5 text-left transition hover:-translate-y-0.5 hover:shadow-lg ${draft.direction==="Dispatched"?"border-violet-500 bg-violet-500/10":"bg-card"}`}><ArrowUpRight className="mb-5 h-6 w-6 text-violet-500"/><p className="font-semibold">Dispatched</p><p className="mt-1 text-sm text-muted-foreground">An item is leaving the institution</p></button></Step>}{stage===1&&<Step title="What is the item?" sub="Capture only the information needed to identify it."><Field label="Item name"><Input value={draft.item} onChange={e=>set("item",e.target.value)} placeholder="e.g. Student certificates"/></Field><Field label="Quantity"><Input type="number" value={draft.qty} onChange={e=>set("qty",e.target.value)} placeholder="0"/></Field><div className="sm:col-span-2"><Field label="Description"><Textarea value={draft.description} onChange={e=>set("description",e.target.value)} placeholder="Short description or contents"/></Field></div></Step>}{stage===2&&<Step title="Where is it going?" sub="Sender fields adapt naturally to the movement direction."><Field label={draft.direction==="Received"?"Sender":"Recipient"}><Input value={draft.party} onChange={e=>set("party",e.target.value)} placeholder="Person or organisation"/></Field><Field label="Phone"><Input value={draft.phone} onChange={e=>set("phone",e.target.value)} placeholder="Contact number"/></Field><Field label="Department"><Input value={draft.department} onChange={e=>set("department",e.target.value)} placeholder="Responsible department"/></Field><Field label="Courier"><Input value={draft.courier} onChange={e=>set("courier",e.target.value)} placeholder="Optional"/></Field><Field label="Tracking number"><Input value={draft.tracking} onChange={e=>set("tracking",e.target.value)} placeholder="Optional"/></Field></Step>}{stage===3&&<Step title="Review & record" sub="Confirm the movement before it enters the register."><div className="sm:col-span-2 rounded-2xl border bg-muted/30 p-5"><p className="text-xs uppercase tracking-wider text-muted-foreground">{draft.direction||"Direction not selected"}</p><p className="mt-2 text-lg font-semibold">{draft.item||"Unnamed item"} <span className="text-sm font-normal text-muted-foreground">× {draft.qty||"0"}</span></p><p className="mt-1 text-sm text-muted-foreground">{draft.party||"Party not added"} · {draft.department||"No department"}</p></div><div className="sm:col-span-2"><Field label="Notes"><Textarea value={draft.notes} onChange={e=>set("notes",e.target.value)} placeholder="Condition, pickup instructions or exceptions"/></Field></div></Step>}</div></CardContent><div className="sticky bottom-0 flex items-center justify-between border-t bg-card/95 p-4"><Button variant="ghost" onClick={save}><Save/>Save draft</Button><div className="flex gap-2">{stage>0&&<Button variant="outline" onClick={()=>setStage(s=>s-1)}><ArrowLeft/>Back</Button>}{stage<3?<Button onClick={()=>setStage(s=>s+1)}>Continue<ArrowRight/></Button>:<Button onClick={submit}><Check/>Record movement</Button>}</div></div></Card>}
 </AppLayout>
}
function Step({title,sub,children}:{title:string;sub:string;children:React.ReactNode}){return <><div className="mb-6"><h2 className="text-xl font-semibold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{sub}</p></div><div className="grid gap-5 sm:grid-cols-2">{children}</div></>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div className="space-y-2"><Label>{label}</Label>{children}</div>}
